// Template switcher
//
// Each template in `templates/` ships with an example CV. Leave exactly one
// line below uncommented to choose which example gets rendered; comment out
// the rest. Run `make` (or `make watch`) to compile this file into main.pdf.

#include "templates/date-column-example.typ"
